type Product {
  id: ID!
  title: String!
  price: Int!
  preview: String!
  images: [String!]
  tags: [String!]
  description: String
}

type Query {
  products: [Product!]!
  product(id: ID!): Product
}
